# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Workflow binary
workflow/alfred-orion-toolkit

# Packed workflow
*.alfredworkflow
